name: virtual_keyboard
version: "0.1.0-dev.0"
description: Keyboard widget for use in widget tree
repository: "https://github.com/surfstudio/SurfGear/tree/dev/packages/virtual_keyboard"
publish_to: none

dependencies:
  flutter:
    sdk: flutter

  ink_widget:
    git:
      url: https://github.com/surfstudio/SurfGear
      path: packages/ink_widget
      ref: feature/ink_widget_null_safety

  surf_lint_rules: ^0.0.2-dev.2

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.12.0-0 <3.0.0"

flutter:

custom:
  is_stable: false
  unstable_version:
  is_plugin: false
  path: virtual_keyboard
  hostUrl:
  separate_repo_url:
